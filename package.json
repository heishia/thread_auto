{
  "name": "thread-auto-monorepo",
  "version": "1.0.0",
  "description": "Thread post auto generator - monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/desktop",
    "build": "npm run build --workspace=apps/desktop",
    "preview": "npm run preview --workspace=apps/desktop",
    "lint": "npm run lint --workspace=apps/desktop",
    "clean": "npm run clean --workspace=apps/desktop"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
